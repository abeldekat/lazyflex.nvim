require("lazy.core.util").warn({
  "`import = lazyflex.hook` has been removed in v3.0.0.",
  "",
  "please use `lazyflex.entry.lazyvim`(when using LazyVim)",
  "or `lazyflex.entry.lazy`(when only using lazy.nvim)",
  "",
  "For other important changes: see issue #55(https://github.com/abeldekat/lazyflex.nvim/issues/55)",
})

return {}
